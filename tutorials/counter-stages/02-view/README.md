# 02-view

This slice adds the View pattern and makes it visible through a screen-like console UI.

Read order:
1. `src/Domain.idr`
2. `src/Main.idr`
3. `scripts/smoke.sh`

Build and smoke:

```sh
pack build
./scripts/smoke.sh
```
